declare namespace ccs {
    class DecorativeDisplay extends ccs.Class {
        constructor();

        ctor();

        getColliderDetector();

        getDisplay();

        getDisplayData();

        init(): Boolean;

        setColliderDetector(colliderDetector: ccs.ColliderDetector);

        setDisplay(display: cc.Node);

        setDisplayData(displayData: ccs.DisplayData);

    }

}
