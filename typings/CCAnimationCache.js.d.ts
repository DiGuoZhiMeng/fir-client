declare namespace cc {
    namespace animationCache {
        function addAnimation(animation: cc.Animation, name: String);

        function addAnimations(plist: String);

        function getAnimation(name: String);

        function removeAnimation(name: String);

    }

}
