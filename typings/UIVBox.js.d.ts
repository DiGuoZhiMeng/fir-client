declare namespace ccui {
    class VBox extends ccui.Layout {
        constructor();

        ctor();

        static create(size: cc.Size);

        ctor(size: cc.Size);

        initWithSize(size: cc.Size): Boolean;

    }

}
