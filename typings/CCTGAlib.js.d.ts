declare namespace cc {
    class BinaryStreamReader {
        constructor(binaryData);

        ctor(binaryData);

    }

    class ImageTGA {
        constructor(status: Number, type: Number, pixelDepth: Number, width: Number, height: Number, imageData: Array<any>, flipped: Number);

        ctor(status: Number, type: Number, pixelDepth: Number, width: Number, height: Number, imageData: Array<any>, flipped: Number);

    }

}
