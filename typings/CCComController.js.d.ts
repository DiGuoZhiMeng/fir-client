declare namespace ccs {
    class ComController extends ccs.Component {
        constructor();

        ctor();
        ctor();

        isEnabled(): Boolean;

        onEnter();

        setEnabled(bool: Boolean);

    }

}
