declare namespace ccs {
    namespace spriteFrameCacheHelper {
        function addSpriteFrameFromFile(plistPath, imagePath);

        function clear();

        function getTextureAtlasWithTexture(texture): any;

    }

}
